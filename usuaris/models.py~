from django.db import models

# Create your models here.
class perfil(models.Model):
    nom  = models.CharField(max_length=100,help_text="Nom del usuari")
    cognoms = models.CharField(max_length=150, help_text="Cognoms del usuari")
    correu = models.